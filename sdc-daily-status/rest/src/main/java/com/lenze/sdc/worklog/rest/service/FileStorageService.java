package com.lenze.sdc.worklog.rest.service;

import org.springframework.stereotype.Service;

@Service
public class FileStorageService {

//	@Autowired
//	FileStorageDaoService storageDaoService;
//	private static final Logger LOGGER = LoggerFactory.getLogger(FileStorageService.class);
//
//	public void store(MultipartFile file) throws IOException {
//		try (InputStream inputStream = file.getInputStream()) {
//			List<WorklogModel> worklogModels = ExcelHelper.extractDataFromExcel(inputStream);
//			LOGGER.info("save worklog to database -> ");
//			worklogModels.forEach(wl -> storageDaoService.saveWorkLog(wl));
//		}
//	}
}
